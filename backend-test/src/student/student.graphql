type Student {
  id: ID!
  name: String!
  dob: String!
  email: String!
}

type Mutation {
  deleteStudent(id: Int!): String
}

type Mutation {
  updateStudent(id: Int!, updatedStudentData: Student!): Student
}
