type Student {
  id: ID!
  name: String!
  dob: String!
  email: String!
}

type Mutation {
  deleteStudent(id: Int!): String
}

#delete below if necessary for update function only

type Mutation {
  updateStudent(id: Int!, updatedStudentData: UpdateStudentInput!): Student!
}

input UpdateStudentInput {
  id: Int!
  name: String
  dob: Date
  email: String
}

type Student {
  id: Int!
  name: String!
  dob: Date!
  email: String!
}
